image:
  file: .gitpod.Dockerfile

ports:
    - port: 5000
      onOpen: open-preview
      visibility: public
tasks:
    - init: >
        python3 -m pip install -r src/requirements.txt
      command: >
        python3 src/SimpleServer.py
